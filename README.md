# trading

schwab trading api wrapper project using a `src` layout.

## Development

Install development dependencies:

```sh
python -m pip install -e ".[dev]"
```

Run linting and formatting checks:

```sh
ruff check .
ruff format --check .
```

Run tests:

```sh
pytest
```
